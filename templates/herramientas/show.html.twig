{% extends 'base.html.twig' %}

{% block title %}Herramientas{% endblock %}

{% block body %}
    <h1>Herramientas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ herramienta.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ herramienta.nombre }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ herramienta.logo }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ herramienta.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_herramientas_index') }}">back to list</a>

    <a href="{{ path('app_herramientas_edit', {'id': herramienta.id}) }}">edit</a>

    {{ include('herramientas/_delete_form.html.twig') }}
{% endblock %}
