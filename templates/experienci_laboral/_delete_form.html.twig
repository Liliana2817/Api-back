<form method="post" action="{{ path('app_experienci_laboral_delete', {'id': experienci_laboral.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ experienci_laboral.id) }}">
    <button class="btn">Delete</button>
</form>
