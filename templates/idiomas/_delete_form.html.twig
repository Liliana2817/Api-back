<form method="post" action="{{ path('app_idiomas_delete', {'id': idioma.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ idioma.id) }}">
    <button class="btn">Delete</button>
</form>
