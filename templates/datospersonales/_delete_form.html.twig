<form method="post" action="{{ path('app_datospersonales_delete', {'id': datospersonale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ datospersonale.id) }}">
    <button class="btn">Delete</button>
</form>
