{% extends 'base.html.twig' %}

{% block title %}Datospersonales index{% endblock %}

{% block body %}
    <h1>Datospersonales index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Datospersonales</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for datospersonale in datospersonales %}
            <tr>
                <td>{{ datospersonale.id }}</td>
                <td>{{ datospersonale.Datospersonales }}</td>
                <td>
                    <a href="{{ path('app_datospersonales_show', {'id': datospersonale.id}) }}">show</a>
                    <a href="{{ path('app_datospersonales_edit', {'id': datospersonale.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_datospersonales_new') }}">Create new</a>
{% endblock %}
