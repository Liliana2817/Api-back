<form method="post" action="{{ path('app_formacion_delete', {'id': formacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formacion.id) }}">
    <button class="btn">Delete</button>
</form>
